import Placeholder from './Placeholder';

/**
 * 作品库页面。步骤 6 实现：
 * - 无限滚动（useWorksInfinite + IntersectionObserver）
 * - 卡片网格 / 列表切换
 * - 排序、URL 筛选、显示模式持久化
 *
 * 筛选参数读取：const { order, sort, circleId } = worksRoute.useSearch();
 */
export default function Works() {
  return <Placeholder title="作品库" />;
}
